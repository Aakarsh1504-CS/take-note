{
  "name": "take-note",
  "version": "2.0.0",
  "private": true,
  "description": "Take-Note — React SPA + Express REST API for personal notes.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev": "npm-run-all --parallel dev:server dev:client",
    "build": "npm install --prefix server && npm install --prefix client && npm run build --prefix client",
    "start": "NODE_ENV=production npm run start --prefix server"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
